language: php
# sudo: false
php:
  - '7.0.10'

services:
  - mysql
  
# env:
#   global:
#     - LC_ALL=en_US.UTF-8

script:
  - cd api
  - composer install
  - php bin/console doctrine:schema:create --env=test
  - php vendor/bin/phpunit