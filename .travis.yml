language: php
# sudo: false
php:
  - '7.0.10'

env:
  - MYSQL_HOST=127.0.0.1 MYSQL_USER=root

services:
  - mysql

script:
  - cd api
  - composer install
  - php bin/console bilemo:fixtures:load
  - php vendor/bin/phpunit